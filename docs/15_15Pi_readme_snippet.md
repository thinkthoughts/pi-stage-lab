## 15 — 15Pi Translation Expand

15Pi expands measurable rate structure into language while preserving meaning.

**Core rule:** a valid translation preserves measurable rate structure in language.

**Translation triplet:** 15Pi expand → 16Pi extend → 17Pi resist.

**Template role:** this notebook begins the translation triplet and preserves the Pi-stage output pattern:

- `docs/` for bridge notes and glossary
- `results/` for CGCS claim scoring and metadata
- `figures/` for reusable visuals
- `math/` for TeX equations

**Mapping function:** `T : {ΔX, Δn, B, C} → language`.

**Translation boundary:** 15Pi is grammar, not application. Translations into photons, CO2, O2, carbon cycle, climate, chemistry, or public-language claims belong in bridge docs and later notebooks.
