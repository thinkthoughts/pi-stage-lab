## 10 — 10Pi System Extend

10Pi extends system structure across scale, coupling strength, and feedback.

**Core rule:** a valid system remains coherent across scale, coupling strength, and feedback.

**System triplet:** 9Pi expand → 10Pi extend → 11Pi resist.

**Template role:** this notebook extends the system triplet and preserves the Pi-stage output pattern:

- `docs/` for bridge notes and glossary
- `results/` for CGCS claim scoring and metadata
- `figures/` for reusable visuals
- `math/` for TeX equations

**System variables:** `L = scale`, `k = coupling strength`, `F = feedback`.

**Translation boundary:** 10Pi is grammar, not application. Translations into photons, CO2, O2, carbon cycle, climate, chemistry, or public-language claims belong in bridge docs and later notebooks.
