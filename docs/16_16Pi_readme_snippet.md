## 16 — 16Pi Translation Extend

16Pi extends translation across contexts, audiences, and representations.

**Core rule:** a valid translation remains consistent across contexts, audiences, and representations.

**Translation triplet:** 15Pi expand → 16Pi extend → 17Pi resist.

**Template role:** this notebook extends the translation triplet and preserves the Pi-stage output pattern:

- `docs/` for bridge notes and glossary
- `results/` for CGCS claim scoring and metadata
- `figures/` for reusable visuals
- `math/` for TeX equations

**Translation context:** `K = context`, `A = audience`, `R = representation`.

**Translation boundary:** 16Pi is grammar, not application. Translations into photons, CO2, O2, carbon cycle, climate, chemistry, or public-language claims belong in bridge docs and later notebooks.
